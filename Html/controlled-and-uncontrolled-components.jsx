// Q9. What are controlled and uncontrolled components?

//. controlled form handled by react state

//uncontrolled form handles by DOM itself using ref

// Controlled
{/* <input value={name} onChange={e => setName(e.target.value)} /> */}

// Uncontrolled
{/* <input ref={inputRef} /> */}
